Liquid Glass confirmation toast that springs in above the tab bar, then auto-hides (you control `open`).

```jsx
<Toast open={shown} message="Added to cart" />
<Toast open={shown} message="Saved to Favorites" icon="ph-fill ph-heart" tone="favorite" />
```
