// Single source of truth for page-level layout values.
// Change here → updates everywhere.

/** Horizontal padding for page content areas */
export const PAGE_CONTENT_PX = "px-4 md:px-[48px] lg:px-[64px] xl:px-[128px]";

/** Gap between product cards in any grid */
export const CARD_GRID_GAP = "gap-2 md:gap-[14px]";

export const NAV_BOTTOM = 85;
export const BAR_BOTTOM = NAV_BOTTOM + 5;
